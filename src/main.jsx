import React from "react";
import ReactDOM from "react-dom/client";
import BiblicalTimeline from "../biblical-timeline.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BiblicalTimeline />
  </React.StrictMode>
);
